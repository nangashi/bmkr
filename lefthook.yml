pre-commit:
  commands:
    fmt-check:
      run: just fmt-check
    gitleaks:
      run: gitleaks protect --staged --verbose

pre-push:
  commands:
    generate:
      run: just generate
      priority: 1
    lint:
      run: just lint
      priority: 2
