pre-commit:
  commands:
    fmt-check:
      run: just fmt-check
    gitleaks:
      run: gitleaks protect --staged --verbose

pre-push:
  commands:
    generate:
      run: just generate
      priority: 1
    check:
      run: just check
      priority: 2
    test:
      run: just test
      priority: 3
