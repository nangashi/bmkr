version: "2"
linters:
  default: standard
  settings:
    generated: strict
formatters:
  enable:
    - gofumpt
    - gci
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/nangashi/bmkr)
