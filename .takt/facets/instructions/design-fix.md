設計レビューで CRITICAL/IMPORTANT と判定された指摘を修正する。

## 手順

1. `{report:design-review.md}` を読み、CRITICAL/IMPORTANT の指摘を確認する
2. 各指摘について、指摘されたファイル・行を Read で確認する
3. 指摘に従い I/F 定義（型・インターフェース・シグネチャ・動作コメント）を修正する
4. 修正後 `go build ./...` / `tsc -b` でコンパイルが通ることを確認する

LOW の指摘は対応不要。
実装本体は書かない（空実装のまま）。
